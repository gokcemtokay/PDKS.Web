@* ============================================ *@
@* Rapor/GenelGirisCikis.cshtml *@

@model IEnumerable<PDKS.Business.DTOs.GirisCikisRaporDTO>
@{
    ViewData["Title"] = "Genel Giriş-Çıkış Raporu";
    var filtre = ViewBag.Filtre as PDKS.Business.DTOs.RaporFiltreDTO;
}

<div class="row mb-3">
    <div class="col-md-8">
        <form method="post" asp-action="GenelGirisCikis" class="row g-3">
            <div class="col-md-4">
                <label class="form-label">Başlangıç Tarihi</label>
                <input type="date" name="BaslangicTarihi" class="form-control"
                       value="@(filtre?.BaslangicTarihi.ToString("yyyy-MM-dd") ?? DateTime.Today.AddMonths(-1).ToString("yyyy-MM-dd"))">
            </div>
            <div class="col-md-4">
                <label class="form-label">Bitiş Tarihi</label>
                <input type="date" name="BitisTarihi" class="form-control"
                       value="@(filtre?.BitisTarihi.ToString("yyyy-MM-dd") ?? DateTime.Today.ToString("yyyy-MM-dd"))">
            </div>
            <div class="col-md-3">
                <label class="form-label">Departman</label>
                <select name="Departman" class="form-select" asp-items="ViewBag.Departmanlar">
                    <option value="">Tümü</option>
                </select>
            </div>
            <div class="col-md-1">
                <label class="form-label">&nbsp;</label>
                <button type="submit" class="btn btn-primary w-100">
                    <i class="fas fa-filter"></i>
                </button>
            </div>
        </form>
    </div>
    <div class="col-md-4 text-end">
        @if (Model != null && Model.Any())
        {
            <label class="form-label">&nbsp;</label>
            <form method="post" asp-action="ExportGirisCikisExcel">
                <input type="hidden" name="BaslangicTarihi" value="@filtre?.BaslangicTarihi" />
                <input type="hidden" name="BitisTarihi" value="@filtre?.BitisTarihi" />
                <input type="hidden" name="Departman" value="@filtre?.Departman" />
                <button type="submit" class="btn btn-success d-block">
                    <i class="fas fa-file-excel"></i> Excel'e Aktar
                </button>
            </form>
        }
    </div>
</div>

@if (Model != null && Model.Any())
{
    <div class="card">
        <div class="card-header">
            <i class="fas fa-chart-line me-2"></i> Genel Giriş-Çıkış Raporu
            <span class="badge bg-light text-dark ms-2">@Model.Count() Kayıt</span>
        </div>
        <div class="card-body">
            <!-- Summary Stats -->
            <div class="row mb-4">
                <div class="col-md-3">
                    <div class="card bg-primary text-white">
                        <div class="card-body text-center">
                            <h3>@Model.Count()</h3>
                            <small>Toplam Kayıt</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card bg-success text-white">
                        <div class="card-body text-center">
                            <h3>@Model.Count(r => r.Durum == "Normal")</h3>
                            <small>Normal Giriş</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card bg-warning text-white">
                        <div class="card-body text-center">
                            <h3>@Model.Count(r => r.GecKalma > 0)</h3>
                            <small>Geç Kalma</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card bg-info text-white">
                        <div class="card-body text-center">
                            <h3>@Model.Count(r => r.FazlaMesaiDakika > 0)</h3>
                            <small>Fazla Mesai</small>
                        </div>
                    </div>
                </div>
            </div>

            <div class="table-responsive">
                <table class="table table-hover table-sm">
                    <thead>
                        <tr>
                            <th>Tarih</th>
                            <th>Personel</th>
                            <th>Sicil No</th>
                            <th>Departman</th>
                            <th>Giriş</th>
                            <th>Çıkış</th>
                            <th>Çalışma</th>
                            <th>Geç Kalma</th>
                            <th>Erken Çıkış</th>
                            <th>Fazla Mesai</th>
                            <th>Durum</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Tarih.ToString("dd.MM.yyyy")</td>
                                <td>@item.PersonelAdi</td>
                                <td><strong>@item.SicilNo</strong></td>
                                <td>@item.Departman</td>
                                <td>@item.GirisSaati?.ToString("HH:mm")</td>
                                <td>@item.CikisSaati?.ToString("HH:mm")</td>
                                <td>@item.ToplamCalismaSuresi</td>
                                <td>
                                    @if (item.GecKalma > 0)
                                    {
                                        <span class="text-warning">@item.GecKalmaSuresi dk</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                                <td>
                                    @if (item.ErkenCikis > 0)
                                    {
                                        <span class="text-info">@item.ErkenCikisSuresi dk</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                                <td>
                                    @if (item.FazlaMesaiDakika > 0)
                                    {
                                        <span class="text-primary">@item.FazlaMesaiDakika dk</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                                <td>
                                    @if (item.Durum == "Normal")
                                    {
                                        <span class="badge bg-success">Normal</span>
                                    }
                                    else if (item.Durum == "Geç Kalmış")
                                    {
                                        <span class="badge bg-warning">Geç</span>
                                    }
                                    else if (item.Durum == "Erken Çıkmış")
                                    {
                                        <span class="badge bg-info">Erken</span>
                                    }
                                    else if (item.Durum == "Fazla Mesai")
                                    {
                                        <span class="badge bg-primary">F.Mesai</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">@item.Durum</span>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}
else
{
    <div class="card">
        <div class="card-body text-center py-5">
            <i class="fas fa-chart-line fa-4x text-muted mb-3"></i>
            <h5>Raporu Görüntülemek İçin Filtre Uygulayın</h5>
            <p class="text-muted">Tarih aralığını seçin ve "Filtrele" butonuna tıklayın.</p>
        </div>
    </div>
}